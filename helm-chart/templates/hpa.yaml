{{- if .Values.restaurant.autoscaling.enabled }}
apiVersion: autoscaling/v2beta1
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "restaurant.fullname" . }}
  labels:
    {{- include "restaurant.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "restaurant.fullname" . }}
  minReplicas: {{ .Values.restaurant.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.restaurant.autoscaling.maxReplicas }}
  metrics:
    {{- if .Values.restaurant.autoscaling.targetCPUUtilizationPercentage }}
    - type: Resource
      resource:
        name: cpu
        targetAverageUtilization: {{ .Values.restaurant.autoscaling.targetCPUUtilizationPercentage }}
    {{- end }}
    {{- if .Values.restaurant.autoscaling.targetMemoryUtilizationPercentage }}
    - type: Resource
      resource:
        name: memory
        targetAverageUtilization: {{ .Values.restaurant.autoscaling.targetMemoryUtilizationPercentage }}
    {{- end }}
{{- end }}
